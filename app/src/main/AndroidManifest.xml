<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.motwkel.miwok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/transilation"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="Numbers" android:name=".Numbers" />
        <activity android:label="Colors"  android:name=".Colors" />
        <activity android:label="Phrases" android:name=".Phrases" />
        <activity android:label="Family"  android:name=".Familly">
        </activity>
    </application>

</manifest>